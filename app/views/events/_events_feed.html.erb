 <div id="eventresult">
 	
 <ul class="events">
          <% @events.each do |e| %>
              <li>
                  <%= link_to e.user.name, e.user %> was going to <%= e.where %> <%= time_ago_in_words(e.created_at) %> ago.
              </li>
          <% end %>
 </ul>
 </div>
