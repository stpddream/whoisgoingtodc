<%= form_for(current_user.friends.build(friended_id: @user.id), remote: true) do |f| %>
  <div><%= f.hidden_field :friended_id %></div>
  <%= f.submit "Add friend", class: "btn btn-large btn-primary" %>
<% end %>