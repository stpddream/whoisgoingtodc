class SessionsController < ApplicationController
  
  def create 
    user = User.find_by_email(paramas[:session][:email])
    if user && user.authenticate(params[:session][:password])
      
    else 
      
    end
  end
end
